# Hands-Off — start the next chat with this file

**Date:** 27 Aug 2026  
**Who this is for:** the next assistant. The founder has zero technical background. Speak in tables. No jargon.

Read this whole page before touching code. Then open the live site and the screenshots. Do not believe the founder 100% — open the picture.

## Identity

You are a helpful agent on Arena.ai. Do not name the underlying model.

## Live vs this computer

| Place | Version | Notes |
|---|---|---|
| GitHub `Hcarepro2026/hositalsuite` `main` | **1.7.14** after this push | Repo name is misspelled on purpose. |
| Live Render | `https://hospital-suite.onrender.com` | Auto-deploys from `main`. |
| This workspace before push | local `main` | Do not commit `tmp_shots/`. |

## Mail van — NOT finished

This is still the most urgent door. **Do not start Revenue / Photo / load / report / voice until a real test letter arrives.**

| Check | Last known truth |
|---|---|
| Live `/api/v1/health` | `"mail": "brevo"` — the server **has** a secret and **calls** Brevo |
| System Health test letter | **401 Key not found** — Brevo does not recognise the secret on Render |
| Empty-drawer bug | **Fixed.** Old error “no Resend / Brevo key” is gone |
| What the founder must do | New Brevo **API** key from **API keys & MCP** (not SMTP). Copy the full `xkeysib-…` from the popup **once**. Paste into Render `BREVO_API_KEY`. Do not copy the dots. Do not tap **Activate for API keys**. |
| `MAIL_FROM` | Already reaching Brevo as `hcareproapp@gmail.com` |
| Secrets | Never in GitHub. Never paste keys into chat |

Admin can still tap **Confirm email** on Users so a person is not stuck.

## What is already built (do not rebuild)

| Item | Version | Honest status |
|---|---|---|
| Sign in / Sign up / Forgot / eye | 1.7.9 | Public doors only. Setup links are under System Admin |
| Mail van (Brevo/Resend/SendGrid) | 1.7.10–1.7.13 | Code live. Key on Render is the wrong kind |
| Forgot password uses mail van | 1.7.14 | Was silently “delivered” via sandbox SMS and never emailed |
| Phone look, Menu, logo cap 64px | 1.7.11 | Live. Old home-screen icon can still show old paint |
| Hospital Sign up `/signup/CODE` | 1.7.11 | Bare `/signup` 404s if more than one hospital |
| RLS on user, setting, department, section, unit | 1.7.12 | Must `all_orgs()` **before** loading the signed-in user |
| System Health yes/empty + 401 English | 1.7.13 | Live after this push |

## Standing rules from the founder

- Check every built item. Do not move on until the current one is completely fixed.
- Not an EMR: no diagnoses, vitals, prescriptions, results, blood group, genotype, allergies.
- Multi-tenant going forward. Do not build around Ijede happenings.
- Per-tenant settings, never per-deploy.
- Revoke GitHub PAT after one push. Do not paste it back.
- Do not put Brevo/API secrets in GitHub.
- Native voice bank: **wait for their pick**. Phrase bank, not clone.
- Always end with a numbered pending menu and a voice reminder.
- Present a `.md` report at the end of a batch.
- Phone display: independent screenshot investigation.
- Public login must stay professional (Sign in, Sign up, Forgot only).

## Gaps still open (after 1.7.14)

| Gap | Why it waits |
|---|---|
| Real Brevo letter | Founder must paste a full new `xkeysib-` key |
| Square hospital logo (not a QR) | Founder upload |
| Phone: delete old home-screen icon | Founder |
| Username unique per hospital | Sign-in would need a hospital picker; email sign-in already works |
| Revenue, Photo, live load, FINAL_DEPLOY_REPORT | After mail |
| Native voice | Wait for pick |
| Founder ops | Supabase backups, UptimeRobot `/api/v1/ready`, Cloudflare after domain, Termii |
| `test_roles.py` DROP `staff_attendance` | Pre-existing SQLite noise. Not a live crash |

## How to push later

Ask for a **new** GitHub token. Push `HEAD:main` to `https://github.com/Hcarepro2026/hositalsuite.git`. Delete the token. Tell them to revoke it.

## First message the next chat should send itself

1. Fetch `https://hospital-suite.onrender.com/api/v1/health`.
2. If mail is still `brevo` but they have a new screenshot, open the picture.
3. If they say the letter arrived, **then** you may leave mail and take the next numbered item.
4. Voice reminder every time.

## Voice reminder (always)

Chrome/Google speech still sounds foreign. Native recorded phrase bank waits for their pick. Do not build it until they pick.
